Application builds as standard Maven project:

    mvn clean install
Unit tests:

    mvn test
Run application:
    
    java -jar target/devtest-1.0-SNAPSHOT.jar source_folder target_output_file
    
